Tests that a tree that contains decisions that all have relationships returns a single tree from the partitioner

Profile Fields:
    ProductType
    Price
    Category
    MaximumAmount
    AuditType

Expected Trees:
    1(ProductType, Price, Category, MaximumAmount, AuditType)